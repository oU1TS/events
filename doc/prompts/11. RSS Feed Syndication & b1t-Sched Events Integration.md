# Chat Conversation: RSS Feed Syndication & b1t-Sched Events Integration

Note: _This is purely the output of the chat conversation and does not contain any raw data, codebase snippets, etc. used to generate the output._

---

### User Input

> I want to use the data shown in this project in C:\Users\gsmur\Documents\GitHub\[oU1TS]\b1t-Sched so the currently active events show up in the events sidebar. What would be the best way to set it up? I am thinking about RSS feeds. in that case, how do I set it up? and is there a better alternative?

### Assistant Analysis & Recommendations

* Inspected both repository architectures:
  - `events` (`c:\Users\gsmur\Documents\GitHub\[oU1TS]\events`): Client-side static SPA with dynamic data files (`raids.json`, `tracker.json`) hosted on GitHub Pages.
  - `b1t-Sched` (`C:\Users\gsmur\Documents\GitHub\[oU1TS]\b1t-Sched`): Task scheduler SPA powered by Firebase Firestore and Vercel serverless functions, featuring an Upcoming Events sidebar (`#events-container` and `#events-container-mobile`).
* Compared architectural approaches:
  1. **Option A (Direct Static JSON Fetching - Recommended for in-app display):** Fetches `https://ou1ts.github.io/events/raids.json` directly with `localStorage` caching. Preserves full schema fidelity (`subEvents`, `fees`, `RegEndDate`, `venue`), requires zero build overhead, and provides instant client-side date filtering.
  2. **Option B (RSS 2.0 XML Feed - Recommended for 3rd party syndication):** Generates standard `feed.xml` on push via GitHub Actions for consumption by external RSS apps, Discord webhooks, and Telegram broadcast bots.
  3. **Option C (Backend Firestore Webhook):** Ingestion via Cloud Functions.
* Outlined technical specifications for both JSON client merging and RSS feed generation.

---

### User Input

> let's set up the RSS feed, so I can use it in other platforms if needed. Help me setup a temporary folder in the C:\Users\gsmur\Documents\GitHub\[oU1TS]\b1t-Sched\ where you will create a follow up guide for the RSS and events sidebar extension.

### Planner Execution & Changes

1. **Created RSS 2.0 Generator Script in `events`**:
   - Implemented `.github/scripts/generate-rss.js` using Node.js filesystem APIs.
   - Evaluates active/future campaigns dynamically against system dates.
   - Converts dates to RFC 822 format.
   - Encapsulates rich HTML summaries (category, dates, deadlines, venue, fee, sub-events, links, deep link) in CDATA blocks.
   - Generates standard RSS 2.0 XML with `<atom:link rel="self" ...>` discovery.
2. **Generated Initial `feed.xml`**:
   - Executed `.github/scripts/generate-rss.js` to compile root `feed.xml`.
3. **HTML Discovery Metadata**:
   - Updated `events/index.html` `<head>` to include `<link rel="alternate" type="application/rss+xml" title="UITS Event Raiders RSS Feed" href="feed.xml">`.
4. **CI/CD Workflow Automation**:
   - Updated `events/.github/workflows/notify.yml` to run `node .github/scripts/generate-rss.js` on every push to `raids.json` or scheduled cron run, staging and committing `feed.xml` alongside `tracker.json`.
5. **Integration Guide for `b1t-Sched`**:
   - Created folder: `C:\Users\gsmur\Documents\GitHub\[oU1TS]\b1t-Sched\doc\rss-events-integration\`.
   - Created comprehensive guide: `INTEGRATION_GUIDE.md` covering:
     - Endpoints (RSS 2.0, JSON, Tracker).
     - Step-by-step implementation for `b1t-Sched` (`js/raids-feed.js`, `js/app.js`, `js/ui.js` badge and deep-link styling).
     - XML RSS parsing implementation using `DOMParser()`.
     - 3rd-party integration setups (Discord webhooks via Zapier/MonitoRSS, Telegram bots, and RSS reader apps).

---

### User Input

> is the rss fullt set up for this project?

### Planner Verification

Confirmed complete end-to-end setup across:
1. `feed.xml` valid RSS 2.0 artifact.
2. `.github/scripts/generate-rss.js` generator script.
3. `index.html` auto-discovery tag.
4. `.github/workflows/notify.yml` CI/CD auto-commit pipeline.

---

### User Input

> update @README.md @learning.md @documentation.md and archive this entire chat in full detail a @doc/prompts with prefix `11.`

### Planner Documentation Updates

1. **Updated [README.md](file:///c:/Users/gsmur/Documents/GitHub/[oU1TS]/events/README.md)**:
   - Added feature item #15 for RSS 2.0 Syndication Feed.
   - Updated project structure diagram with `.github/scripts/generate-rss.js` and `feed.xml`.
   - Added live RSS feed link in the Community & Links section.
2. **Updated [documentation.md](file:///c:/Users/gsmur/Documents/GitHub/[oU1TS]/events/documentation.md)**:
   - Added `Section 3.L (RSS 2.0 Syndication Feed Subsystem)` detailing compliance, script execution, discovery, and cross-platform integrations.
   - Updated directory tree.
   - Added `v1.11.0` entry to Version History and Changelog.
3. **Updated [learning.md](file:///c:/Users/gsmur/Documents/GitHub/[oU1TS]/events/learning.md)**:
   - Added `RSS 2.0 Feed Generator & Syndication Subsystem` under Module Breakdown explaining data flows, XML escaping, CDATA payloads, and UX benefits.
   - Added row to the UX Impact Matrix.
4. **Archived Chat**:
   - Saved full conversation log at `doc/prompts/11. RSS Feed Syndication & b1t-Sched Events Integration.md`.
